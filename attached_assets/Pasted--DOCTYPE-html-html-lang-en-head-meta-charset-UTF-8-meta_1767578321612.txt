<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your First 5K | Why Racing Events</title>
    <link href="https://fonts.googleapis.com/css2?family=Italiana&family=Syne:wght@400;500;600;700;800&family=Space+Grotesk:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --crimson: #C41E3A;
            --crimson-dark: #9B1B30;
            --crimson-glow: rgba(196, 30, 58, 0.12);
            --midnight: #0B1120;
            --navy: #141E30;
            --ink: #1A1F35;
            --cream: #FDF9F6;
            --ivory: #FFFBF7;
            --sand: #F5EDE4;
            --champagne: #F7E7CE;
            --gold: #D4AF37;
            --bronze: #CD7F32;
            --charcoal: #2D2D3A;
            --slate: #4A4A5C;
            --mist: #8A8A9C;
            --emerald: #1B7340;
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }
        
        html { 
            scroll-behavior: smooth;
            font-size: 16px;
        }
        
        body { 
            font-family: 'Space Grotesk', sans-serif;
            background: var(--ivory);
            color: var(--charcoal);
            line-height: 1.7;
            overflow-x: hidden;
            -webkit-font-smoothing: antialiased;
        }

        /* CUSTOM CURSOR */
        .cursor {
            width: 20px;
            height: 20px;
            border: 2px solid var(--crimson);
            border-radius: 50%;
            position: fixed;
            pointer-events: none;
            z-index: 9999;
            transition: transform 0.15s ease, opacity 0.15s ease;
            mix-blend-mode: difference;
        }

        .cursor.hover {
            transform: scale(2);
            background: rgba(196, 30, 58, 0.1);
        }

        /* TYPOGRAPHY */
        h1, h2, h3 { 
            font-family: 'Italiana', serif;
            font-weight: 400;
            line-height: 1.05;
        }

        .display-text {
            font-family: 'Syne', sans-serif;
            font-weight: 800;
            text-transform: uppercase;
            letter-spacing: -0.02em;
        }

        /* ORGANIC SHAPES */
        .blob-1 {
            position: absolute;
            width: 800px;
            height: 800px;
            background: radial-gradient(ellipse at center, var(--crimson-glow) 0%, transparent 70%);
            border-radius: 40% 60% 70% 30% / 40% 50% 60% 50%;
            filter: blur(60px);
            animation: morphBlob 20s ease-in-out infinite;
            pointer-events: none;
        }

        .blob-2 {
            position: absolute;
            width: 600px;
            height: 600px;
            background: radial-gradient(ellipse at center, rgba(212, 175, 55, 0.08) 0%, transparent 70%);
            border-radius: 60% 40% 30% 70% / 60% 30% 70% 40%;
            filter: blur(50px);
            animation: morphBlob 15s ease-in-out infinite reverse;
            pointer-events: none;
        }

        @keyframes morphBlob {
            0%, 100% { border-radius: 40% 60% 70% 30% / 40% 50% 60% 50%; transform: rotate(0deg) scale(1); }
            25% { border-radius: 60% 40% 30% 70% / 50% 60% 40% 60%; }
            50% { border-radius: 30% 60% 70% 40% / 50% 40% 60% 50%; transform: rotate(180deg) scale(1.1); }
            75% { border-radius: 50% 40% 50% 60% / 40% 60% 40% 60%; }
        }

        /* GRAIN OVERLAY */
        .grain {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: 10000;
            opacity: 0.03;
            background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 512 512' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.8' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)'/%3E%3C/svg%3E");
        }

        /* NAV */
        nav {
            position: fixed;
            top: 0;
            width: 100%;
            padding: 28px 5%;
            display: flex;
            justify-content: space-between;
            align-items: center;
            z-index: 1000;
            transition: all 0.6s cubic-bezier(0.16, 1, 0.3, 1);
        }

        nav.scrolled {
            padding: 18px 5%;
            background: rgba(255, 251, 247, 0.85);
            backdrop-filter: blur(30px) saturate(180%);
            -webkit-backdrop-filter: blur(30px) saturate(180%);
            border-bottom: 1px solid rgba(0,0,0,0.04);
        }

        .logo { 
            font-family: 'Syne', sans-serif;
            font-size: 15px; 
            font-weight: 800; 
            color: var(--midnight); 
            letter-spacing: 5px;
            text-transform: uppercase;
        }

        .logo span { 
            color: var(--crimson);
            position: relative;
        }

        .logo span::after {
            content: '';
            position: absolute;
            bottom: -2px;
            left: 0;
            width: 100%;
            height: 2px;
            background: var(--crimson);
        }
        
        .nav-links { 
            display: flex; 
            gap: 48px; 
            align-items: center; 
        }
        
        .nav-links a { 
            font-size: 12px; 
            font-weight: 600;
            letter-spacing: 2px;
            text-transform: uppercase;
            color: var(--slate);
            transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
            position: relative;
        }

        .nav-links a::before {
            content: '';
            position: absolute;
            bottom: -8px;
            left: 50%;
            width: 4px;
            height: 4px;
            background: var(--crimson);
            border-radius: 50%;
            transform: translateX(-50%) scale(0);
            transition: transform 0.4s cubic-bezier(0.16, 1, 0.3, 1);
        }

        .nav-links a:hover { color: var(--crimson); }
        .nav-links a:hover::before { transform: translateX(-50%) scale(1); }

        /* HERO */
        .hero {
            min-height: 100vh;
            display: grid;
            grid-template-columns: 1fr 1.2fr;
            position: relative;
            overflow: hidden;
        }

        .hero .blob-1 {
            top: -20%;
            left: -10%;
        }

        .hero .blob-2 {
            bottom: -30%;
            right: 20%;
        }

        .hero-content {
            display: flex;
            flex-direction: column;
            justify-content: center;
            padding: 180px 8% 120px 10%;
            position: relative;
            z-index: 2;
        }

        .hero-tag {
            display: inline-flex;
            align-items: center;
            gap: 16px;
            margin-bottom: 40px;
        }

        .hero-tag-line {
            width: 60px;
            height: 1px;
            background: linear-gradient(90deg, var(--crimson), transparent);
        }

        .hero-tag-text {
            font-size: 11px;
            font-weight: 700;
            letter-spacing: 5px;
            text-transform: uppercase;
            color: var(--crimson);
        }

        .hero-title {
            font-size: clamp(4rem, 8vw, 7rem);
            color: var(--midnight);
            margin-bottom: 12px;
            overflow: hidden;
        }

        .hero-title .line {
            display: block;
            overflow: hidden;
        }

        .hero-title .word {
            display: inline-block;
            transform: translateY(100%);
            animation: revealWord 1.2s cubic-bezier(0.16, 1, 0.3, 1) forwards;
        }

        .hero-title .word:nth-child(1) { animation-delay: 0.1s; }
        .hero-title .word:nth-child(2) { animation-delay: 0.2s; }
        .hero-title .word:nth-child(3) { animation-delay: 0.3s; }
        .hero-title .word:nth-child(4) { animation-delay: 0.4s; }

        @keyframes revealWord {
            to { transform: translateY(0); }
        }

        .hero-title em {
            font-family: 'Italiana', serif;
            font-style: italic;
            color: var(--crimson);
        }

        .hero-subtitle-accent {
            font-family: 'Syne', sans-serif;
            font-size: clamp(1rem, 2vw, 1.4rem);
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 4px;
            color: var(--mist);
            margin-bottom: 36px;
        }

        .hero-desc {
            font-size: 1.15rem;
            color: var(--slate);
            max-width: 480px;
            line-height: 1.9;
            margin-bottom: 50px;
        }

        .hero-quote-wrap {
            position: relative;
            max-width: 520px;
            margin-bottom: 56px;
        }

        .hero-quote-deco {
            position: absolute;
            top: -30px;
            left: -20px;
            font-family: 'Italiana', serif;
            font-size: 12rem;
            line-height: 1;
            color: var(--sand);
            z-index: -1;
            opacity: 0.6;
        }

        .hero-quote {
            background: white;
            padding: 36px 40px;
            border-radius: 4px;
            position: relative;
            box-shadow: 
                0 1px 2px rgba(0,0,0,0.02),
                0 4px 8px rgba(0,0,0,0.02),
                0 16px 32px rgba(0,0,0,0.04),
                0 32px 64px rgba(0,0,0,0.04);
        }

        .hero-quote::before {
            content: '';
            position: absolute;
            left: 0;
            top: 0;
            bottom: 0;
            width: 4px;
            background: linear-gradient(180deg, var(--crimson), var(--gold));
            border-radius: 4px 0 0 4px;
        }

        .hero-quote p {
            font-family: 'Italiana', serif;
            font-size: 1.35rem;
            color: var(--charcoal);
            line-height: 1.6;
            font-style: italic;
        }

        .hero-quote cite {
            display: flex;
            align-items: center;
            gap: 12px;
            font-family: 'Space Grotesk', sans-serif;
            font-size: 0.8rem;
            font-style: normal;
            font-weight: 600;
            letter-spacing: 2px;
            text-transform: uppercase;
            color: var(--mist);
            margin-top: 20px;
        }

        .hero-quote cite::before {
            content: '';
            width: 24px;
            height: 1px;
            background: var(--mist);
        }

        .hero-cta {
            display: inline-flex;
            align-items: center;
            gap: 20px;
            background: var(--crimson);
            color: white;
            padding: 24px 48px;
            border-radius: 60px;
            font-weight: 700;
            font-size: 12px;
            text-transform: uppercase;
            letter-spacing: 4px;
            transition: all 0.5s cubic-bezier(0.16, 1, 0.3, 1);
            position: relative;
            overflow: hidden;
        }

        .hero-cta::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, rgba(255,255,255,0.2) 0%, transparent 50%);
            opacity: 0;
            transition: opacity 0.4s;
        }

        .hero-cta:hover::before { opacity: 1; }

        .hero-cta:hover {
            transform: translateY(-4px) scale(1.02);
            box-shadow: 
                0 20px 40px rgba(196, 30, 58, 0.25),
                0 40px 80px rgba(196, 30, 58, 0.15);
        }

        .hero-cta i {
            font-size: 14px;
            transition: transform 0.4s cubic-bezier(0.16, 1, 0.3, 1);
        }

        .hero-cta:hover i {
            transform: translateX(6px);
        }

        /* HERO IMAGE SIDE */
        .hero-visual {
            position: relative;
            overflow: hidden;
        }

        .hero-image-mask {
            position: absolute;
            inset: 60px;
            border-radius: 300px 300px 20px 20px;
            overflow: hidden;
            box-shadow: 
                0 40px 80px rgba(0,0,0,0.12),
                0 80px 160px rgba(0,0,0,0.08);
        }

        .hero-image-mask img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 12s cubic-bezier(0.16, 1, 0.3, 1);
        }

        .hero-image-mask:hover img {
            transform: scale(1.08);
        }

        .hero-image-mask::after {
            content: '';
            position: absolute;
            inset: 0;
            background: linear-gradient(180deg, 
                rgba(11, 17, 32, 0.1) 0%, 
                transparent 30%,
                transparent 60%,
                rgba(11, 17, 32, 0.15) 100%);
            pointer-events: none;
        }

        .hero-float-card {
            position: absolute;
            background: white;
            border-radius: 16px;
            box-shadow: 
                0 8px 24px rgba(0,0,0,0.06),
                0 24px 48px rgba(0,0,0,0.06);
            z-index: 10;
        }

        .hero-stat-card {
            bottom: 100px;
            left: 20px;
            padding: 36px 44px;
        }

        .hero-stat-number {
            font-family: 'Italiana', serif;
            font-size: 4rem;
            color: var(--midnight);
            line-height: 1;
            margin-bottom: 8px;
        }

        .hero-stat-label {
            font-size: 0.85rem;
            color: var(--mist);
            line-height: 1.5;
        }

        .hero-badge-card {
            top: 100px;
            right: 80px;
            padding: 18px 28px;
            background: var(--emerald);
            color: white;
            border-radius: 60px;
            display: flex;
            align-items: center;
            gap: 12px;
            font-weight: 700;
            font-size: 13px;
            letter-spacing: 1px;
            box-shadow: 0 16px 40px rgba(27, 115, 64, 0.35);
        }

        /* MARQUEE */
        .marquee-section {
            padding: 40px 0;
            background: var(--midnight);
            overflow: hidden;
            position: relative;
        }

        .marquee-track {
            display: flex;
            animation: marquee 30s linear infinite;
        }

        @keyframes marquee {
            0% { transform: translateX(0); }
            100% { transform: translateX(-50%); }
        }

        .marquee-item {
            flex-shrink: 0;
            padding: 0 60px;
            font-family: 'Italiana', serif;
            font-size: 1.8rem;
            color: white;
            opacity: 0.7;
            white-space: nowrap;
        }

        .marquee-item span {
            color: var(--crimson);
            margin: 0 20px;
        }

        /* SECTION STYLING */
        .section {
            padding: 160px 8%;
            position: relative;
        }

        .section-header {
            text-align: center;
            max-width: 750px;
            margin: 0 auto 100px;
        }

        .section-tag {
            display: inline-flex;
            align-items: center;
            gap: 20px;
            margin-bottom: 28px;
        }

        .section-tag-line {
            width: 40px;
            height: 1px;
            background: var(--crimson);
        }

        .section-tag-text {
            font-size: 11px;
            font-weight: 700;
            letter-spacing: 5px;
            text-transform: uppercase;
            color: var(--crimson);
        }

        .section-title {
            font-size: clamp(3rem, 6vw, 4.5rem);
            color: var(--midnight);
            margin-bottom: 28px;
        }

        .section-subtitle {
            font-size: 1.15rem;
            color: var(--mist);
            line-height: 1.9;
        }

        /* FEARS SECTION */
        .fears-section {
            background: white;
        }

        .fears-section .blob-1 {
            top: 20%;
            right: -20%;
            opacity: 0.5;
        }

        .fears-bento {
            display: grid;
            grid-template-columns: repeat(6, 1fr);
            grid-template-rows: repeat(2, auto);
            gap: 24px;
            max-width: 1400px;
            margin: 0 auto;
        }

        .fear-card {
            background: var(--cream);
            padding: 44px 36px;
            border-radius: 20px;
            position: relative;
            overflow: hidden;
            transition: all 0.6s cubic-bezier(0.16, 1, 0.3, 1);
        }

        .fear-card:nth-child(1) { grid-column: span 2; }
        .fear-card:nth-child(2) { grid-column: span 2; }
        .fear-card:nth-child(3) { grid-column: span 2; }
        .fear-card:nth-child(4) { grid-column: span 3; }
        .fear-card:nth-child(5) { grid-column: span 3; }

        .fear-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: linear-gradient(90deg, var(--crimson), var(--gold));
            transform: scaleX(0);
            transform-origin: left;
            transition: transform 0.6s cubic-bezier(0.16, 1, 0.3, 1);
        }

        .fear-card:hover::before { transform: scaleX(1); }

        .fear-card:hover {
            transform: translateY(-12px);
            box-shadow: 
                0 24px 48px rgba(0,0,0,0.06),
                0 48px 96px rgba(0,0,0,0.04);
        }

        .fear-num {
            font-family: 'Italiana', serif;
            font-size: 5rem;
            line-height: 1;
            color: var(--sand);
            position: absolute;
            top: 20px;
            right: 28px;
        }

        .fear-voice {
            font-family: 'Italiana', serif;
            font-size: 1.5rem;
            color: var(--charcoal);
            margin-bottom: 20px;
            line-height: 1.4;
            font-style: italic;
            position: relative;
            z-index: 1;
        }

        .fear-truth {
            font-size: 0.95rem;
            color: var(--slate);
            line-height: 1.8;
            position: relative;
            z-index: 1;
        }

        .fear-truth strong {
            color: var(--emerald);
            font-weight: 700;
        }

        /* PERMISSION SECTION */
        .permission-section {
            background: var(--midnight);
            color: white;
            position: relative;
            overflow: hidden;
        }

        .permission-section .blob-1 {
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            opacity: 0.8;
            width: 1200px;
            height: 1200px;
        }

        .permission-section .section-tag-line,
        .permission-section .section-tag-text {
            color: var(--gold);
            background: var(--gold);
        }

        .permission-section .section-title {
            color: white;
        }

        .permission-grid {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 20px;
            max-width: 1100px;
            margin: 0 auto 80px;
            position: relative;
            z-index: 1;
        }

        .permission-item {
            text-align: center;
            padding: 40px 28px;
            background: rgba(255,255,255,0.03);
            border: 1px solid rgba(255,255,255,0.06);
            border-radius: 20px;
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            transition: all 0.5s cubic-bezier(0.16, 1, 0.3, 1);
        }

        .permission-item:hover {
            background: rgba(255,255,255,0.08);
            transform: translateY(-8px);
            border-color: rgba(255,255,255,0.12);
        }

        .permission-icon {
            width: 64px;
            height: 64px;
            margin: 0 auto 20px;
            background: linear-gradient(135deg, var(--crimson), var(--crimson-dark));
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.4rem;
            box-shadow: 0 12px 32px rgba(196, 30, 58, 0.3);
        }

        .permission-text {
            font-size: 1rem;
            font-weight: 500;
            line-height: 1.5;
        }

        .permission-quote-wrap {
            max-width: 700px;
            margin: 0 auto;
            text-align: center;
            position: relative;
            z-index: 1;
        }

        .permission-divider {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 24px;
            margin-bottom: 40px;
            opacity: 0.3;
        }

        .permission-divider span {
            width: 80px;
            height: 1px;
            background: white;
        }

        .permission-quote {
            font-family: 'Italiana', serif;
            font-size: 2rem;
            line-height: 1.5;
            font-style: italic;
        }

        /* STORIES */
        .stories-section {
            background: var(--sand);
        }

        .stories-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 40px;
            max-width: 1400px;
            margin: 0 auto;
        }

        .story-card {
            background: white;
            border-radius: 24px;
            overflow: hidden;
            transition: all 0.7s cubic-bezier(0.16, 1, 0.3, 1);
        }

        .story-card:hover {
            transform: translateY(-20px) rotate(-1deg);
            box-shadow: 
                0 40px 80px rgba(0,0,0,0.1),
                0 80px 160px rgba(0,0,0,0.06);
        }

        .story-card:nth-child(2) {
            transform: translateY(40px);
        }

        .story-card:nth-child(2):hover {
            transform: translateY(20px) rotate(1deg);
        }

        .story-image {
            height: 320px;
            position: relative;
            overflow: hidden;
        }

        .story-image img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 1s cubic-bezier(0.16, 1, 0.3, 1);
        }

        .story-card:hover .story-image img {
            transform: scale(1.1);
        }

        .story-image::after {
            content: '';
            position: absolute;
            inset: 0;
            background: linear-gradient(180deg, transparent 50%, rgba(0,0,0,0.4) 100%);
        }

        .story-content {
            padding: 40px;
        }

        .story-before {
            font-size: 0.9rem;
            color: var(--mist);
            font-style: italic;
            margin-bottom: 20px;
            padding-bottom: 20px;
            border-bottom: 1px solid var(--sand);
        }

        .story-quote {
            font-family: 'Italiana', serif;
            font-size: 1.35rem;
            color: var(--charcoal);
            line-height: 1.6;
            margin-bottom: 32px;
        }

        .story-author {
            display: flex;
            align-items: center;
            gap: 16px;
        }

        .story-avatar {
            width: 60px;
            height: 60px;
            border-radius: 50%;
            object-fit: cover;
            border: 3px solid var(--cream);
        }

        .story-name {
            font-weight: 700;
            font-size: 0.95rem;
            color: var(--charcoal);
        }

        .story-detail {
            font-size: 0.85rem;
            color: var(--crimson);
            font-weight: 600;
            letter-spacing: 1px;
        }

        /* EXPECT */
        .expect-section {
            background: var(--ivory);
        }

        .expect-layout {
            display: grid;
            grid-template-columns: 1.1fr 1fr;
            gap: 120px;
            align-items: center;
            max-width: 1400px;
            margin: 0 auto;
        }

        .expect-content .section-title {
            text-align: left;
            margin-bottom: 20px;
        }

        .expect-intro {
            font-size: 1.1rem;
            color: var(--mist);
            line-height: 1.9;
            margin-bottom: 56px;
        }

        .expect-steps {
            display: flex;
            flex-direction: column;
            gap: 20px;
        }

        .expect-step {
            display: flex;
            gap: 28px;
            padding: 32px 36px;
            background: white;
            border-radius: 20px;
            transition: all 0.5s cubic-bezier(0.16, 1, 0.3, 1);
            position: relative;
            overflow: hidden;
        }

        .expect-step::before {
            content: '';
            position: absolute;
            left: 0;
            top: 0;
            bottom: 0;
            width: 0;
            background: linear-gradient(180deg, var(--crimson), var(--gold));
            transition: width 0.5s cubic-bezier(0.16, 1, 0.3, 1);
        }

        .expect-step:hover::before { width: 5px; }

        .expect-step:hover {
            transform: translateX(16px);
            box-shadow: 
                -16px 16px 40px rgba(0,0,0,0.04),
                -32px 32px 80px rgba(0,0,0,0.03);
        }

        .expect-num {
            width: 56px;
            height: 56px;
            background: var(--cream);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: 'Italiana', serif;
            font-size: 1.6rem;
            color: var(--crimson);
            flex-shrink: 0;
        }

        .expect-step-title {
            font-size: 0.85rem;
            font-weight: 700;
            letter-spacing: 3px;
            text-transform: uppercase;
            color: var(--midnight);
            margin-bottom: 8px;
        }

        .expect-step-desc {
            font-size: 0.95rem;
            color: var(--slate);
            line-height: 1.7;
        }

        .expect-visual {
            position: relative;
        }

        .expect-image {
            border-radius: 300px 300px 40px 40px;
            overflow: hidden;
            box-shadow: 
                0 40px 80px rgba(0,0,0,0.1),
                0 80px 160px rgba(0,0,0,0.06);
        }

        .expect-image img {
            width: 100%;
            height: 700px;
            object-fit: cover;
        }

        .expect-badge {
            position: absolute;
            bottom: 60px;
            left: -60px;
            background: var(--emerald);
            color: white;
            padding: 24px 36px;
            border-radius: 60px;
            font-weight: 700;
            font-size: 14px;
            letter-spacing: 2px;
            display: flex;
            align-items: center;
            gap: 14px;
            box-shadow: 0 20px 50px rgba(27, 115, 64, 0.4);
        }

        /* JOURNEY */
        .journey-section {
            background: linear-gradient(180deg, white 0%, var(--cream) 100%);
        }

        .journey-track {
            display: flex;
            justify-content: space-between;
            max-width: 1200px;
            margin: 100px auto 0;
            padding: 0 40px;
            position: relative;
        }

        .journey-track::before {
            content: '';
            position: absolute;
            top: 70px;
            left: 120px;
            right: 120px;
            height: 3px;
            background: linear-gradient(90deg, 
                var(--crimson) 0%, 
                var(--gold) 50%,
                var(--midnight) 100%);
            border-radius: 2px;
        }

        .journey-node {
            text-align: center;
            flex: 1;
            position: relative;
            z-index: 1;
        }

        .journey-icon-wrap {
            width: 140px;
            height: 140px;
            margin: 0 auto 32px;
            position: relative;
        }

        .journey-icon-ring {
            position: absolute;
            inset: 0;
            border: 2px dashed var(--champagne);
            border-radius: 50%;
            animation: spinSlow 30s linear infinite;
        }

        @keyframes spinSlow {
            to { transform: rotate(360deg); }
        }

        .journey-icon {
            position: absolute;
            inset: 15px;
            background: white;
            border: 3px solid var(--crimson);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 2.2rem;
            color: var(--crimson);
            box-shadow: 0 16px 40px rgba(0,0,0,0.06);
            transition: all 0.5s cubic-bezier(0.16, 1, 0.3, 1);
        }

        .journey-node:hover .journey-icon {
            transform: scale(1.15);
            box-shadow: 0 20px 50px rgba(196, 30, 58, 0.2);
        }

        .journey-node:last-child .journey-icon {
            border-color: var(--midnight);
            color: var(--midnight);
        }

        .journey-label {
            font-size: 0.75rem;
            font-weight: 700;
            letter-spacing: 4px;
            text-transform: uppercase;
            color: var(--midnight);
            margin-bottom: 12px;
        }

        .journey-feeling {
            font-family: 'Italiana', serif;
            font-size: 1.15rem;
            font-style: italic;
            color: var(--mist);
            max-width: 120px;
            margin: 0 auto;
        }

        /* FINAL CTA */
        .final-section {
            background: linear-gradient(135deg, var(--midnight) 0%, #050810 100%);
            color: white;
            text-align: center;
            padding: 200px 8%;
            position: relative;
            overflow: hidden;
        }

        .final-section .blob-1 {
            top: 50%;
            left: 30%;
            transform: translate(-50%, -50%);
            opacity: 1;
            width: 1000px;
            height: 1000px;
        }

        .final-section .blob-2 {
            top: 50%;
            left: 70%;
            transform: translate(-50%, -50%);
            opacity: 0.8;
        }

        .final-content {
            position: relative;
            z-index: 1;
            max-width: 900px;
            margin: 0 auto;
        }

        .final-section .section-tag-line,
        .final-section .section-tag-text {
            color: var(--gold);
            background: var(--gold);
        }

        .final-title {
            font-size: clamp(3rem, 6vw, 5rem);
            margin-bottom: 32px;
            line-height: 1.1;
        }

        .final-title em {
            font-style: italic;
            color: var(--crimson);
        }

        .final-subtitle {
            font-size: 1.2rem;
            opacity: 0.7;
            max-width: 580px;
            margin: 0 auto 64px;
            line-height: 1.9;
        }

        .final-btn {
            display: inline-flex;
            align-items: center;
            gap: 24px;
            background: var(--crimson);
            color: white;
            padding: 28px 64px;
            border-radius: 60px;
            font-weight: 700;
            font-size: 13px;
            text-transform: uppercase;
            letter-spacing: 4px;
            transition: all 0.6s cubic-bezier(0.16, 1, 0.3, 1);
            position: relative;
            overflow: hidden;
        }

        .final-btn::before {
            content: '';
            position: absolute;
            inset: 0;
            background: linear-gradient(135deg, rgba(255,255,255,0.25) 0%, transparent 50%);
            opacity: 0;
            transition: opacity 0.4s;
        }

        .final-btn:hover::before { opacity: 1; }

        .final-btn:hover {
            transform: translateY(-6px) scale(1.03);
            box-shadow: 
                0 24px 48px rgba(196, 30, 58, 0.35),
                0 48px 96px rgba(196, 30, 58, 0.2);
        }

        .final-btn i {
            transition: transform 0.4s cubic-bezier(0.16, 1, 0.3, 1);
        }

        .final-btn:hover i { transform: translateX(8px); }

        .trust-row {
            display: flex;
            justify-content: center;
            gap: 64px;
            margin-top: 72px;
        }

        .trust-item {
            display: flex;
            align-items: center;
            gap: 14px;
            font-size: 0.9rem;
            font-weight: 500;
            opacity: 0.6;
        }

        .trust-item i {
            color: var(--gold);
            font-size: 1rem;
        }

        /* FOOTER */
        footer {
            background: #030508;
            color: white;
            padding: 100px 8% 60px;
            text-align: center;
        }

        .footer-logo {
            font-family: 'Syne', sans-serif;
            font-size: 18px;
            font-weight: 800;
            letter-spacing: 6px;
            margin-bottom: 12px;
        }

        .footer-logo span { color: var(--crimson); }

        .footer-tagline {
            font-family: 'Italiana', serif;
            font-size: 1.1rem;
            font-style: italic;
            opacity: 0.35;
            margin-bottom: 48px;
        }

        .footer-links {
            display: flex;
            justify-content: center;
            gap: 56px;
            margin-bottom: 48px;
        }

        .footer-links a {
            font-size: 11px;
            font-weight: 600;
            letter-spacing: 3px;
            text-transform: uppercase;
            opacity: 0.4;
            transition: all 0.4s;
        }

        .footer-links a:hover {
            opacity: 1;
            color: var(--crimson);
        }

        .footer-copy {
            font-size: 0.75rem;
            opacity: 0.25;
            letter-spacing: 1px;
        }

        /* RESPONSIVE */
        @media (max-width: 1200px) {
            .hero {
                grid-template-columns: 1fr;
            }

            .hero-content {
                text-align: center;
                padding: 200px 8% 100px;
            }

            .hero-tag { justify-content: center; }
            .hero-quote-wrap { margin: 0 auto 56px; }
            .hero-cta { margin: 0 auto; }

            .hero-visual {
                min-height: 600px;
            }

            .hero-image-mask {
                inset: 40px;
                border-radius: 200px 200px 20px 20px;
            }

            .fears-bento {
                grid-template-columns: 1fr 1fr;
            }

            .fear-card:nth-child(n) { grid-column: span 1; }
            .fear-card:nth-child(4),
            .fear-card:nth-child(5) { grid-column: span 1; }

            .permission-grid {
                grid-template-columns: repeat(2, 1fr);
            }

            .expect-layout {
                grid-template-columns: 1fr;
                gap: 80px;
            }

            .expect-visual { order: -1; }

            .journey-track {
                flex-direction: column;
                gap: 60px;
            }

            .journey-track::before { display: none; }
        }

        @media (max-width: 900px) {
            .stories-grid {
                grid-template-columns: 1fr;
                max-width: 500px;
            }

            .story-card:nth-child(2) { transform: none; }
            .story-card:nth-child(2):hover { transform: translateY(-20px); }

            .trust-row {
                flex-direction: column;
                gap: 24px;
            }

            .nav-links { display: none; }
        }

        @media (max-width: 768px) {
            .fears-bento {
                grid-template-columns: 1fr;
            }

            .permission-grid {
                grid-template-columns: 1fr;
                max-width: 320px;
            }

            .expect-badge {
                left: 20px;
            }
        }

        /* SCROLL ANIMATIONS */
        .reveal {
            opacity: 0;
            transform: translateY(60px);
            transition: all 1s cubic-bezier(0.16, 1, 0.3, 1);
        }

        .reveal.visible {
            opacity: 1;
            transform: translateY(0);
        }

        .reveal-delay-1 { transition-delay: 0.1s; }
        .reveal-delay-2 { transition-delay: 0.2s; }
        .reveal-delay-3 { transition-delay: 0.3s; }
        .reveal-delay-4 { transition-delay: 0.4s; }
        .reveal-delay-5 { transition-delay: 0.5s; }
    </style>
</head>
<body>

<div class="grain"></div>
<div class="cursor" id="cursor"></div>

<!-- NAV -->
<nav id="navbar">
    <a href="#" class="logo">WHY <span>RACING</span></a>
    <div class="nav-links">
        <a href="#">Events</a>
        <a href="#">Training</a>
        <a href="#">Results</a>
        <a href="#">Contact</a>
    </div>
</nav>

<!-- HERO -->
<section class="hero">
    <div class="blob-1"></div>
    <div class="blob-2"></div>
    
    <div class="hero-content">
        <div class="hero-tag">
            <span class="hero-tag-line"></span>
            <span class="hero-tag-text">For First-Timers</span>
        </div>

        <h1 class="hero-title">
            <span class="line"><span class="word">What</span> <span class="word">If</span> <span class="word">You</span></span>
            <span class="line"><span class="word">Actually</span> <span class="word"><em>Could?</em></span></span>
        </h1>

        <p class="hero-subtitle-accent">The finish line is closer than you think</p>

        <p class="hero-desc">
            You don't need to be faster, fitter, or more ready. You just need to show up. We'll be there at the finish line—and so will the version of you that's been waiting to emerge.
        </p>

        <div class="hero-quote-wrap">
            <span class="hero-quote-deco">"</span>
            <blockquote class="hero-quote">
                <p>Every finisher at our races was once exactly where you are right now—wondering if they could actually do this.</p>
                <cite>Coach Sarah, Why Racing Events</cite>
            </blockquote>
        </div>

        <a href="#final" class="hero-cta">
            Find My First Race
            <i class="fas fa-arrow-right"></i>
        </a>
    </div>

    <div class="hero-visual">
        <div class="hero-badge-card">
            <i class="fas fa-check"></i>
            Walking Welcome
        </div>
        <div class="hero-image-mask">
            <img src="https://images.unsplash.com/photo-1552674605-db6ffd4facb5?w=1200&q=80" alt="Runners crossing finish line">
        </div>
        <div class="hero-float-card hero-stat-card">
            <div class="hero-stat-number">12,847</div>
            <div class="hero-stat-label">First-timers crossed our<br>finish lines in 2024</div>
        </div>
    </div>
</section>

<!-- MARQUEE -->
<div class="marquee-section">
    <div class="marquee-track">
        <span class="marquee-item">You Belong Here <span>✦</span></span>
        <span class="marquee-item">Walking Is Welcome <span>✦</span></span>
        <span class="marquee-item">No Time Cutoffs <span>✦</span></span>
        <span class="marquee-item">Every Pace Celebrated <span>✦</span></span>
        <span class="marquee-item">First-Timers Welcome <span>✦</span></span>
        <span class="marquee-item">You Belong Here <span>✦</span></span>
        <span class="marquee-item">Walking Is Welcome <span>✦</span></span>
        <span class="marquee-item">No Time Cutoffs <span>✦</span></span>
        <span class="marquee-item">Every Pace Celebrated <span>✦</span></span>
        <span class="marquee-item">First-Timers Welcome <span>✦</span></span>
    </div>
</div>

<!-- FEARS -->
<section class="section fears-section">
    <div class="blob-1"></div>
    
    <div class="section-header">
        <div class="section-tag">
            <span class="section-tag-line"></span>
            <span class="section-tag-text">You're Not Alone</span>
            <span class="section-tag-line"></span>
        </div>
        <h2 class="section-title">That Voice In Your Head?<br>Everyone Hears It.</h2>
        <p class="section-subtitle">Here are the worries we hear most—and the truth that might surprise you.</p>
    </div>

    <div class="fears-bento">
        <div class="fear-card reveal reveal-delay-1">
            <span class="fear-num">01</span>
            <div class="fear-voice">"What if I'm the slowest one there?"</div>
            <div class="fear-truth"><strong>Reality:</strong> Every race has a wide range of paces. Many people walk the entire thing. The only person tracking your time is you.</div>
        </div>

        <div class="fear-card reveal reveal-delay-2">
            <span class="fear-num">02</span>
            <div class="fear-voice">"What if I have to walk?"</div>
            <div class="fear-truth"><strong>Reality:</strong> Walking isn't cheating—it's smart pacing. Even experienced runners take walk breaks. At our events, it's encouraged.</div>
        </div>

        <div class="fear-card reveal reveal-delay-3">
            <span class="fear-num">03</span>
            <div class="fear-voice">"I'm not a 'real runner'"</div>
            <div class="fear-truth"><strong>Reality:</strong> If you run, you're a runner. No minimum speed, no required body type, no experience needed.</div>
        </div>

        <div class="fear-card reveal reveal-delay-4">
            <span class="fear-num">04</span>
            <div class="fear-voice">"What if I can't finish? I don't know what to wear or do."</div>
            <div class="fear-truth"><strong>Reality:</strong> If you can walk 45 minutes, you can finish. Wear what you train in. Arrive early. We send everything you need to know—no surprises.</div>
        </div>

        <div class="fear-card reveal reveal-delay-5">
            <span class="fear-num">05</span>
            <div class="fear-voice">"People will judge me."</div>
            <div class="fear-truth"><strong>Reality:</strong> The running community cheers loudest for the back of the pack. You'll feel welcomed, not judged. We promise.</div>
        </div>
    </div>
</section>

<!-- PERMISSION -->
<section class="section permission-section">
    <div class="blob-1"></div>
    
    <div class="section-header">
        <div class="section-tag">
            <span class="section-tag-line"></span>
            <span class="section-tag-text">A Reminder</span>
            <span class="section-tag-line"></span>
        </div>
        <h2 class="section-title">Permission Granted</h2>
    </div>

    <div class="permission-grid">
        <div class="permission-item reveal reveal-delay-1">
            <div class="permission-icon"><i class="fas fa-shoe-prints"></i></div>
            <div class="permission-text">To walk when you need to</div>
        </div>
        <div class="permission-item reveal reveal-delay-2">
            <div class="permission-icon"><i class="fas fa-hourglass-half"></i></div>
            <div class="permission-text">To be slow</div>
        </div>
        <div class="permission-item reveal reveal-delay-3">
            <div class="permission-icon"><i class="fas fa-heart"></i></div>
            <div class="permission-text">To be nervous</div>
        </div>
        <div class="permission-item reveal reveal-delay-4">
            <div class="permission-icon"><i class="fas fa-expand-arrows-alt"></i></div>
            <div class="permission-text">To take up space</div>
        </div>
        <div class="permission-item reveal reveal-delay-1">
            <div class="permission-icon"><i class="fas fa-seedling"></i></div>
            <div class="permission-text">To start before ready</div>
        </div>
        <div class="permission-item reveal reveal-delay-2">
            <div class="permission-icon"><i class="fas fa-running"></i></div>
            <div class="permission-text">To call yourself a runner</div>
        </div>
        <div class="permission-item reveal reveal-delay-3">
            <div class="permission-icon"><i class="fas fa-tint"></i></div>
            <div class="permission-text">To cry at the finish</div>
        </div>
        <div class="permission-item reveal reveal-delay-4">
            <div class="permission-icon"><i class="fas fa-star"></i></div>
            <div class="permission-text">To be proud of yourself</div>
        </div>
    </div>

    <div class="permission-quote-wrap">
        <div class="permission-divider">
            <span></span>
            <i class="fas fa-diamond"></i>
            <span></span>
        </div>
        <p class="permission-quote reveal">"The only person you need to beat is the voice telling you not to try."</p>
    </div>
</section>

<!-- STORIES -->
<section class="section stories-section">
    <div class="section-header">
        <div class="section-tag">
            <span class="section-tag-line"></span>
            <span class="section-tag-text">Real Stories</span>
            <span class="section-tag-line"></span>
        </div>
        <h2 class="section-title">They Were Exactly<br>Where You Are</h2>
        <p class="section-subtitle">First-timers who showed up scared—and left transformed.</p>
    </div>

    <div class="stories-grid">
        <div class="story-card reveal reveal-delay-1">
            <div class="story-image">
                <img src="https://images.unsplash.com/photo-1571019613454-1cb2f99b2d8b?w=800&q=80" alt="Runner">
            </div>
            <div class="story-content">
                <div class="story-before">"I hadn't run since high school PE—20 years ago."</div>
                <div class="story-quote">"When I crossed that finish line, I cried. Not because I was fast—I wasn't. But because I did something I never thought I could."</div>
                <div class="story-author">
                    <img class="story-avatar" src="https://images.unsplash.com/photo-1438761681033-6461ffad8d80?w=100&q=80" alt="Jennifer">
                    <div>
                        <div class="story-name">Jennifer M.</div>
                        <div class="story-detail">First 5K at age 42</div>
                    </div>
                </div>
            </div>
        </div>

        <div class="story-card reveal reveal-delay-2">
            <div class="story-image">
                <img src="https://images.unsplash.com/photo-1594882645126-14020914d58d?w=800&q=80" alt="Runner">
            </div>
            <div class="story-content">
                <div class="story-before">"I almost backed out the morning of."</div>
                <div class="story-quote">"I looked around and saw every body type, every age. I wasn't alone. The cheers from strangers actually made me tear up."</div>
                <div class="story-author">
                    <img class="story-avatar" src="https://images.unsplash.com/photo-1507003211169-0a1dd7228f2d?w=100&q=80" alt="Marcus">
                    <div>
                        <div class="story-name">Marcus T.</div>
                        <div class="story-detail">First 5K at age 38</div>
                    </div>
                </div>
            </div>
        </div>

        <div class="story-card reveal reveal-delay-3">
            <div class="story-image">
                <img src="https://images.unsplash.com/photo-1486218119243-13883505764c?w=800&q=80" alt="Runner">
            </div>
            <div class="story-content">
                <div class="story-before">"I told myself I'd never be a runner."</div>
                <div class="story-quote">"I walked most of it. Nobody cared. That medal means more to me than any trophy ever could."</div>
                <div class="story-author">
                    <img class="story-avatar" src="https://images.unsplash.com/photo-1544005313-94ddf0286df2?w=100&q=80" alt="Rachel">
                    <div>
                        <div class="story-name">Rachel K.</div>
                        <div class="story-detail">First 5K at age 51</div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- EXPECT -->
<section class="section expect-section">
    <div class="expect-layout">
        <div class="expect-content">
            <h2 class="section-title">Here's Exactly What<br>To Expect</h2>
            <p class="expect-intro">No surprises. No guesswork. Just a clear picture of race day so you can focus on crossing that finish line.</p>

            <div class="expect-steps">
                <div class="expect-step reveal reveal-delay-1">
                    <div class="expect-num">1</div>
                    <div>
                        <div class="expect-step-title">Week Before</div>
                        <div class="expect-step-desc">You'll get an email with everything: parking, timing, what to bring. We answer every question before you have to ask.</div>
                    </div>
                </div>

                <div class="expect-step reveal reveal-delay-2">
                    <div class="expect-num">2</div>
                    <div>
                        <div class="expect-step-title">Race Morning</div>
                        <div class="expect-step-desc">Arrive 45 minutes early. Pick up your bib. Use the restroom. Feel the nervous energy—that's completely normal.</div>
                    </div>
                </div>

                <div class="expect-step reveal reveal-delay-3">
                    <div class="expect-num">3</div>
                    <div>
                        <div class="expect-step-title">The Start</div>
                        <div class="expect-step-desc">Line up toward the back. When the horn sounds, don't sprint. Start slower than you think you should.</div>
                    </div>
                </div>

                <div class="expect-step reveal reveal-delay-4">
                    <div class="expect-num">4</div>
                    <div>
                        <div class="expect-step-title">The Finish</div>
                        <div class="expect-step-desc">Cross the line. Grab your medal. Take the photo. You just did something most people only talk about.</div>
                    </div>
                </div>
            </div>
        </div>

        <div class="expect-visual reveal">
            <div class="expect-image">
                <img src="https://images.unsplash.com/photo-1476480862126-209bfaa8edc8?w=1000&q=80" alt="Finish line">
            </div>
            <div class="expect-badge">
                <i class="fas fa-check-circle"></i>
                Walking 100% Welcome
            </div>
        </div>
    </div>
</section>

<!-- JOURNEY -->
<section class="section journey-section">
    <div class="section-header">
        <div class="section-tag">
            <span class="section-tag-line"></span>
            <span class="section-tag-text">Your Path</span>
            <span class="section-tag-line"></span>
        </div>
        <h2 class="section-title">The 8-Week Journey</h2>
        <p class="section-subtitle">From "I can't run" to "I just did."</p>
    </div>

    <div class="journey-track">
        <div class="journey-node reveal reveal-delay-1">
            <div class="journey-icon-wrap">
                <div class="journey-icon-ring"></div>
                <div class="journey-icon"><i class="fas fa-seedling"></i></div>
            </div>
            <div class="journey-label">Week 1-2</div>
            <div class="journey-feeling">"Can I really do this?"</div>
        </div>

        <div class="journey-node reveal reveal-delay-2">
            <div class="journey-icon-wrap">
                <div class="journey-icon-ring"></div>
                <div class="journey-icon"><i class="fas fa-shoe-prints"></i></div>
            </div>
            <div class="journey-label">Week 3-4</div>
            <div class="journey-feeling">"Hard, but getting better"</div>
        </div>

        <div class="journey-node reveal reveal-delay-3">
            <div class="journey-icon-wrap">
                <div class="journey-icon-ring"></div>
                <div class="journey-icon"><i class="fas fa-bolt"></i></div>
            </div>
            <div class="journey-label">Week 5-6</div>
            <div class="journey-feeling">"I might actually do this"</div>
        </div>

        <div class="journey-node reveal reveal-delay-4">
            <div class="journey-icon-wrap">
                <div class="journey-icon-ring"></div>
                <div class="journey-icon"><i class="fas fa-heart"></i></div>
            </div>
            <div class="journey-label">Week 7-8</div>
            <div class="journey-feeling">"Nervous, excited, ready"</div>
        </div>

        <div class="journey-node reveal reveal-delay-5">
            <div class="journey-icon-wrap">
                <div class="journey-icon-ring"></div>
                <div class="journey-icon"><i class="fas fa-trophy"></i></div>
            </div>
            <div class="journey-label">Race Day</div>
            <div class="journey-feeling">"I DID IT."</div>
        </div>
    </div>
</section>

<!-- FINAL -->
<section class="final-section" id="final">
    <div class="blob-1"></div>
    <div class="blob-2"></div>
    
    <div class="final-content">
        <div class="section-tag" style="justify-content: center;">
            <span class="section-tag-line"></span>
            <span class="section-tag-text">The Moment Is Now</span>
            <span class="section-tag-line"></span>
        </div>

        <h2 class="final-title reveal">The Finish Line Is Waiting.<br><em>So Is Who You'll Become.</em></h2>
        <p class="final-subtitle reveal reveal-delay-1">You've thought about it long enough. Today's the day you stop wondering and start becoming.</p>
        
        <a href="#" class="final-btn reveal reveal-delay-2">
            I'm In — Find My First Race
            <i class="fas fa-arrow-right"></i>
        </a>

        <div class="trust-row reveal reveal-delay-3">
            <div class="trust-item">
                <i class="fas fa-exchange-alt"></i>
                Free transfers if life happens
            </div>
            <div class="trust-item">
                <i class="fas fa-medal"></i>
                Finisher medal guaranteed
            </div>
            <div class="trust-item">
                <i class="fas fa-infinity"></i>
                No time cutoffs
            </div>
        </div>
    </div>
</section>

<!-- FOOTER -->
<footer>
    <div class="footer-logo">WHY <span>RACING</span></div>
    <div class="footer-tagline">What's Your Why?</div>
    <div class="footer-links">
        <a href="#">Events</a>
        <a href="#">Training</a>
        <a href="#">Results</a>
        <a href="#">Volunteer</a>
        <a href="#">Contact</a>
    </div>
    <div class="footer-copy">© 2025 Why Racing Events Inc. • La Center, WA • (360) 314-4682</div>
</footer>

<script>
// Custom cursor
const cursor = document.getElementById('cursor');
let mouseX = 0, mouseY = 0, cursorX = 0, cursorY = 0;

document.addEventListener('mousemove', e => {
    mouseX = e.clientX;
    mouseY = e.clientY;
});

function animateCursor() {
    cursorX += (mouseX - cursorX) * 0.15;
    cursorY += (mouseY - cursorY) * 0.15;
    cursor.style.left = cursorX - 10 + 'px';
    cursor.style.top = cursorY - 10 + 'px';
    requestAnimationFrame(animateCursor);
}
animateCursor();

document.querySelectorAll('a, button, .fear-card, .story-card, .permission-item, .expect-step').forEach(el => {
    el.addEventListener('mouseenter', () => cursor.classList.add('hover'));
    el.addEventListener('mouseleave', () => cursor.classList.remove('hover'));
});

// Nav scroll
window.addEventListener('scroll', () => {
    document.getElementById('navbar').classList.toggle('scrolled', window.scrollY > 60);
});

// Smooth scroll
document.querySelectorAll('a[href^="#"]').forEach(anchor => {
    anchor.addEventListener('click', e => {
        e.preventDefault();
        document.querySelector(anchor.getAttribute('href'))?.scrollIntoView({ behavior: 'smooth' });
    });
});

// Reveal on scroll
const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
        if (entry.isIntersecting) {
            entry.target.classList.add('visible');
        }
    });
}, { threshold: 0.1, rootMargin: '0px 0px -100px 0px' });

document.querySelectorAll('.reveal').forEach(el => observer.observe(el));

// Parallax on scroll
window.addEventListener('scroll', () => {
    const scrolled = window.scrollY;
    document.querySelectorAll('.blob-1, .blob-2').forEach((blob, i) => {
        const speed = i === 0 ? 0.03 : 0.02;
        blob.style.transform = `translate(${-50 + scrolled * speed}%, ${-50 + scrolled * speed}%)`;
    });
});
</script>

</body>
</html>