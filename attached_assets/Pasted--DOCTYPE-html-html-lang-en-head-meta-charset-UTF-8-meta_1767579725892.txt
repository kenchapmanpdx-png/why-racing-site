<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your First 5K | Why Racing Events</title>
    <link href="https://fonts.googleapis.com/css2?family=Fraunces:ital,opsz,wght@0,9..144,300;0,9..144,400;0,9..144,600;1,9..144,400;1,9..144,600&family=Inter:wght@400;500;600;700&family=Oswald:wght@500;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        /* --- INTEGRATED BRANDING --- */
        :root {
            /* Official Why Racing Colors */
            --brand-red: #E31837;    
            --brand-blue: #00205B;
            
            /* Page Specific Palette (Softer/Warmer) */
            --cream: #FAF7F2;
            --white: #FFFFFF;
            --sand: #F0E9DF;
            --gold: #D4AF37;
            --charcoal: #2C2C2C;
            --slate: #5A5A6E;
            --mist: #8E8E9A;
            --green: #1B7340;
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }
        
        html { 
            scroll-behavior: smooth;
            -webkit-text-size-adjust: 100%;
        }
        
        body { 
            font-family: 'Inter', sans-serif;
            background: var(--white);
            color: var(--charcoal);
            line-height: 1.7;
            overflow-x: hidden;
        }

        /* Fraunces for Emotional Headers, Oswald for Brand Headers */
        h1, h2 { font-family: 'Fraunces', serif; font-weight: 400; }
        h3, h4 { font-family: 'Inter', sans-serif; font-weight: 700; }
        
        /* Utility */
        a { text-decoration: none; color: inherit; transition: 0.3s; }
        img { max-width: 100%; height: auto; display: block; }
        
        /* --- NAVIGATION (MATCHES MAIN SITE) --- */
        nav {
            position: fixed;
            top: 0;
            width: 100%;
            padding: 15px 5%;
            display: flex;
            justify-content: space-between;
            align-items: center;
            z-index: 1000;
            background: rgba(255,255,255,0.98);
            border-bottom: 1px solid rgba(0,0,0,0.05);
            backdrop-filter: blur(10px);
        }

        .logo {
            font-family: 'Oswald', sans-serif; /* Brand Font */
            font-size: 24px; 
            font-weight: 700; 
            color: var(--brand-blue); 
            letter-spacing: 1px;
            text-transform: uppercase;
            font-style: italic;
        }

        .logo span { color: var(--brand-red); }

        .nav-links { display: none; gap: 30px; }
        .nav-links a { 
            font-size: 14px; 
            font-weight: 700; 
            text-transform: uppercase; 
            color: var(--brand-blue); 
            font-family: 'Oswald', sans-serif;
            letter-spacing: 1px;
        }
        
        .btn-nav {
            background: var(--brand-red);
            color: white !important;
            padding: 8px 24px;
            border-radius: 4px;
        }

        /* --- HERO SECTION --- */
        .hero {
            padding: 140px 20px 80px;
            background: linear-gradient(180deg, var(--cream) 0%, var(--white) 100%);
        }

        .hero-inner {
            max-width: 1200px;
            margin: 0 auto;
            display: grid;
            gap: 60px;
        }

        .hero-badge {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            background: rgba(227, 24, 55, 0.1);
            padding: 8px 16px;
            border-radius: 100px;
            font-size: 12px;
            font-weight: 700;
            color: var(--brand-red);
            letter-spacing: 1px;
            text-transform: uppercase;
            margin-bottom: 24px;
            font-family: 'Oswald', sans-serif;
        }

        .hero-title {
            font-size: 3rem;
            color: var(--brand-blue);
            line-height: 1.1;
            margin-bottom: 24px;
        }

        .hero-title em {
            font-style: italic;
            color: var(--brand-red);
            font-family: 'Fraunces', serif; /* Emotional Emphasis */
        }

        .hero-desc {
            font-size: 1.1rem;
            color: var(--slate);
            margin-bottom: 32px;
            max-width: 500px;
        }

        .hero-quote {
            border-left: 4px solid var(--brand-red);
            padding-left: 20px;
            margin-bottom: 32px;
            font-style: italic;
            color: var(--charcoal);
        }

        .hero-cta {
            background: var(--brand-red);
            color: white;
            padding: 18px 36px;
            border-radius: 4px; /* Matches Brand Shape */
            font-weight: 700;
            font-family: 'Oswald', sans-serif;
            text-transform: uppercase;
            letter-spacing: 1px;
            display: inline-flex;
            align-items: center;
            gap: 10px;
            box-shadow: 0 4px 15px rgba(227, 24, 55, 0.3);
        }

        .hero-cta:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(227, 24, 55, 0.4);
        }

        .hero-image {
            position: relative;
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 20px 40px rgba(0,0,0,0.1);
        }

        .walking-badge {
            position: absolute;
            top: 20px; right: 20px;
            background: white;
            padding: 10px 20px;
            border-radius: 50px;
            font-weight: 700;
            color: var(--green);
            font-size: 12px;
            display: flex;
            align-items: center;
            gap: 6px;
            box-shadow: 0 4px 10px rgba(0,0,0,0.1);
        }

        /* --- FEARS SECTION --- */
        .section { padding: 100px 20px; }
        .bg-light { background: var(--cream); }

        .section-header { text-align: center; max-width: 700px; margin: 0 auto 60px; }
        .section-label { color: var(--brand-red); font-weight: 700; text-transform: uppercase; letter-spacing: 2px; font-size: 12px; margin-bottom: 10px; font-family: 'Oswald', sans-serif;}
        .section-title { font-size: 2.5rem; color: var(--brand-blue); margin-bottom: 15px; }

        .fears-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 24px;
            max-width: 1100px;
            margin: 0 auto;
        }

        .fear-card {
            background: white;
            padding: 30px;
            border-radius: 12px;
            border: 1px solid rgba(0,0,0,0.05);
            transition: 0.3s;
        }

        .fear-card:hover { transform: translateY(-5px); box-shadow: 0 10px 30px rgba(0,0,0,0.05); }

        .fear-voice {
            font-family: 'Fraunces', serif;
            font-size: 1.2rem;
            color: var(--brand-blue);
            font-style: italic;
            margin-bottom: 15px;
            padding-bottom: 15px;
            border-bottom: 1px solid var(--sand);
        }

        .fear-truth strong { color: var(--green); }

        /* --- PERMISSION SECTION (The Brene Brown "Shame Tape" Counter) --- */
        .permission-section {
            background: var(--brand-blue);
            color: white;
            text-align: center;
        }

        .permission-section .section-title { color: white; }

        .permission-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 15px;
            max-width: 800px;
            margin: 0 auto 40px;
        }

        .permission-item {
            background: rgba(255,255,255,0.05);
            padding: 20px;
            border-radius: 8px;
            border: 1px solid rgba(255,255,255,0.1);
        }
        
        .permission-item i { font-size: 1.5rem; color: var(--brand-red); display: block; margin-bottom: 10px; }
        
        /* --- TIMELINE --- */
        .timeline-track {
            max-width: 900px;
            margin: 0 auto;
            position: relative;
            padding: 20px 0;
        }
        
        .timeline-track::before {
            content:''; position: absolute; top:0; bottom:0; left: 24px; width: 2px; background: var(--sand);
        }

        .timeline-item {
            display: flex;
            gap: 30px;
            margin-bottom: 40px;
            position: relative;
        }

        .timeline-icon {
            width: 50px; height: 50px;
            background: white;
            border: 2px solid var(--brand-red);
            border-radius: 50%;
            display: flex; align-items: center; justify-content: center;
            color: var(--brand-red);
            z-index: 2;
            flex-shrink: 0;
        }

        /* --- FINAL CTA --- */
        .final-cta {
            background: var(--brand-blue);
            color: white;
            text-align: center;
            padding: 100px 20px;
            position: relative;
            overflow: hidden;
        }

        .trust-badges {
            display: flex;
            justify-content: center;
            gap: 30px;
            margin-top: 40px;
            opacity: 0.7;
            font-size: 0.9rem;
        }

        .trust-badges div { display: flex; align-items: center; gap: 8px; }

        /* Responsive */
        @media (min-width: 768px) {
            .nav-links { display: flex; }
            .hero-inner { grid-template-columns: 1fr 1fr; align-items: center; }
            .permission-grid { grid-template-columns: repeat(4, 1fr); }
            .hero-title { font-size: 3.5rem; }
            .timeline-track::before { left: 50%; transform: translateX(-50%); }
            .timeline-item { width: 50%; margin-left: auto; padding-left: 40px; }
            .timeline-item:nth-child(even) { margin-left: 0; margin-right: auto; flex-direction: row-reverse; padding-right: 40px; padding-left: 0; text-align: right; }
            .timeline-icon { position: absolute; left: 50%; transform: translateX(-50%); }
        }
    </style>
</head>
<body>

    <nav>
        <div class="logo">WHY <span>RACING</span></div>
        <div class="nav-links">
            <a href="#">Events</a>
            <a href="#">Training</a>
            <a href="#">Results</a>
            <a href="#" class="btn-nav">Find A Race</a>
        </div>
    </nav>

    <section class="hero">
        <div class="hero-inner">
            <div class="hero-content">
                <div class="hero-badge"><i class="fas fa-heart"></i> For First-Timers</div>
                <h1 class="hero-title">What If You Actually <em>Could?</em></h1>
                <p class="hero-desc">You don't need to be faster, fitter, or more ready. You just need to show up. We'll be there at the finish line—and so will the version of you that's been waiting to emerge.</p>
                <div class="hero-quote">"Every finisher at our races was once exactly where you are right now—wondering if they could actually do this."</div>
                <a href="#final" class="hero-cta">Find My First Race <i class="fas fa-arrow-right"></i></a>
            </div>
            <div class="hero-image">
                <div class="walking-badge"><i class="fas fa-check"></i> Walking 100% Welcome</div>
                <img src="https://images.unsplash.com/photo-1552674605-db6ffd4facb5?w=1000&q=80" alt="Runners at finish line">
            </div>
        </div>
    </section>

    <section class="section bg-light">
        <div class="section-header">
            <div class="section-label">You Are Not Alone</div>
            <h2 class="section-title">That Voice In Your Head? <br>Everyone Hears It.</h2>
        </div>
        <div class="fears-grid">
            <div class="fear-card">
                <div class="fear-voice">"What if I'm the slowest one there?"</div>
                <div class="fear-truth"><strong>The Reality:</strong> Every race has a wide range of paces. Many people walk the entire thing. The only person tracking your time is you.</div>
            </div>
            <div class="fear-card">
                <div class="fear-voice">"I'm not a 'real runner'."</div>
                <div class="fear-truth"><strong>The Reality:</strong> If you run (or walk-run, or jog), you're a runner. No minimum speed required. You belong here.</div>
            </div>
            <div class="fear-card">
                <div class="fear-voice">"People will judge me."</div>
                <div class="fear-truth"><strong>The Reality:</strong> The running community cheers loudest for the back of the pack. You will feel welcomed, not judged.</div>
            </div>
        </div>
    </section>

    <section class="section permission-section">
        <div class="section-header">
            <div class="section-label" style="color: var(--gold);">A Reminder</div>
            <h2 class="section-title">Permission Granted</h2>
            <p style="opacity: 0.8;">The only person you need to beat is the voice telling you not to try.</p>
        </div>
        <div class="permission-grid">
            <div class="permission-item">
                <i class="fas fa-shoe-prints"></i>
                <div>To walk when you need to</div>
            </div>
            <div class="permission-item">
                <i class="fas fa-hourglass-half"></i>
                <div>To be slow</div>
            </div>
            <div class="permission-item">
                <i class="fas fa-expand-arrows-alt"></i>
                <div>To take up space</div>
            </div>
            <div class="permission-item">
                <i class="fas fa-tint"></i>
                <div>To cry at the finish</div>
            </div>
        </div>
    </section>

    <section class="section">
        <div class="section-header">
            <div class="section-label">The Journey</div>
            <h2 class="section-title">From "I Can't" to "I Did"</h2>
        </div>
        <div class="timeline-track">
            <div class="timeline-item">
                <div class="timeline-icon"><i class="fas fa-seedling"></i></div>
                <div>
                    <h4>Week 1-2</h4>
                    <p>"Can I really do this?" You start moving. It's hard, but you show up.</p>
                </div>
            </div>
            <div class="timeline-item">
                <div class="timeline-icon"><i class="fas fa-bolt"></i></div>
                <div>
                    <h4>Week 5-6</h4>
                    <p>"I might actually do this." You run a little further without stopping.</p>
                </div>
            </div>
            <div class="timeline-item">
                <div class="timeline-icon"><i class="fas fa-trophy"></i></div>
                <div>
                    <h4>Race Day</h4>
                    <p>"I DID IT." The finish line. The medal. The new identity.</p>
                </div>
            </div>
        </div>
    </section>

    <section class="final-cta" id="final">
        <div class="section-header">
            <h2 class="section-title" style="color: white; font-size: 3rem;">The Finish Line Is Waiting.<br><em>So Is Who You'll Become.</em></h2>
            <p style="color: rgba(255,255,255,0.8);">You've thought about it long enough. Today is the day you stop wondering and start becoming.</p>
        </div>
        <a href="#" class="hero-cta" style="background: white; color: var(--brand-red);">I'm In — Find My First Race</a>
        
        <div class="trust-badges">
            <div><i class="fas fa-medal"></i> Guaranteed Medal</div>
            <div><i class="fas fa-exchange-alt"></i> Free Transfers</div>
            <div><i class="fas fa-infinity"></i> No Time Cutoffs</div>
        </div>
    </section>

</body>
</html>